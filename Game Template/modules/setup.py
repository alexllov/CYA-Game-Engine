"""
This file is currently required to setup things before the core game file is
ran. Its purpose is to serve as a place to import optional modules, 
instantiate objects from those modules, and assign variable ids to the
objects and modules (optional).
"""

import inventory
i = inventory.Inventory()